from ssb_dash_framework.utils.r_helpers import _get_kostra_r

# import subprocess
# commands = [
#     "R -e 'renv::init()'",
#     # "renv::install('statisticsnorway/ssb-kostra')"
# ]

# for command in commands:
#     subprocess.run(
#         command,
#         shell=True,
#         check=True
#     )

print(_get_kostra_r())